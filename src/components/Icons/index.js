const { default: BarIcon } = require('./BarIcon');
const { default: BellIcon } = require('./BellIcon');
const { default: MessageIcon } = require('./MessageIcon');
const { default: ProfileIcon } = require('./ProfileIcon');

export { MessageIcon, BellIcon, ProfileIcon, BarIcon };
